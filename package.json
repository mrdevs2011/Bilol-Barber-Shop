{
  "name": "bilol-barber-shop",
  "version": "1.0.0",
  "private": true,
  "description": "Bilol Barber Shop — statik sayt + Vercel serverless (notify-admin) + Supabase (auth, bookings, edge functions).",
  "scripts": {
    "build": "node scripts/generate-config.js"
  }
}
